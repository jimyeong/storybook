const express = require("express");
const router = express.Router();
/*

router.get("/", (req, res)=>{
    res.send("dashboard")
});
*/

/*

router.get("/dashboard", (req, res)=>{
    let state = {}
    state.user = req.user;
    res.render("index/dashboard", state);
});
*/

module.exports = router;


